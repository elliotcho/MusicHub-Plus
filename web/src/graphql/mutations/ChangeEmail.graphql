mutation ChangeEmail($newEmail: String!) {
    changeEmail(newEmail: $newEmail) {
         user {
            id
            username
        }

        errors {
            field 
            message
        }
    }
}