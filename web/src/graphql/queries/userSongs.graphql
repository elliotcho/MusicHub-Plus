query UserSongs($limit: Int!, $cursor: String){
    userSongs(limit: $limit, cursor: $cursor) {
       hasMore
       songs {
            id
            title
            url
            ratingStatus
            createdAt
            dislikes
            likes
            user {
                id
                username
            }
       }
    }
}